<p style="color: green"><%= notice %></p>
<div class="container">
  <%= render @application %>

  <div>
    <%= link_to "Edit this application", edit_account_application_path(@application) %> |
    <%= link_to "Back to account", account_path(account_id: @application.account_id) %>

    <%= button_to "Destroy this application", account_application_path(account_id: @application.account_id, id: @application.id), method: :delete %>
  </div>
</div>
